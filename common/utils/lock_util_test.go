package utils

import (
	"testing"
)

func TestLock_tryLock(t *testing.T) {
	//InitRedis()
	//lock := Lock{"key", "v", config.GetRDB(), 10}
	//locked, err := lock.TryLock()
	//if err != nil {
	//	return
	//}
	//t.Log(locked)
}
